{
   "convertable" : true,
   "jps"         : "https://raw.githubusercontent.com/SiryjVyiko/tomcat-cluster/master/manifest.yml",
   "required"    : false,
   "defaultState": false,
   "description" : "Tomcat clustering provides all-to-all session session and context attribute replication all over the nodes of the layer using the DeltaManager to replicate session deltas.",
   "validation": {
        "minCount": 2
   }
}
